DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/enjoyyoga
CORS_ORIGINS=["http://localhost:3000"]

# SMTP Email Settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_FROM_EMAIL=your-email@gmail.com
SMTP_FROM_NAME=enjoyyoga
SMTP_USE_TLS=true
# SMTP_PASSWORD: Do NOT store in this file. Use macOS Keychain for local dev:
#   security add-generic-password -a "enjoyyoga" -s "smtp-password" -w "your-gmail-app-password"
# Then start the server with:
#   export SMTP_PASSWORD=$(security find-generic-password -a "enjoyyoga" -s "smtp-password" -w)
#   uv run uvicorn app.main:app --reload
